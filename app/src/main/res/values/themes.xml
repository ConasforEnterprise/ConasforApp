<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->

    <style name="Base.Theme.ConasforApp" parent="Theme.Material3.DayNight.NoActionBar">

        <!--
        <item name="android:statusBarColor">@color/colorStatusBar</item>
        <item name="android:navigationBarColor">@color/black</item>
        -->

        <item name="actionModeStyle">@style/MyActionMode</item>
        <!--
        <item name="android:navigationBarColor">@android:color/transparent</item>
        -->
        <item name="android:statusBarColor">@color/colorStatusBar</item>
        <item name="android:navigationBarColor">@color/black</item>
        <!--
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
        -->

    </style>

    <style name="Theme.ConasforApp" parent="Base.Theme.ConasforApp" />

    <color name="colorStatusBar">#FF4FCA16</color>
    <color name="navigationBarColor">#4FCA16</color>

</resources>