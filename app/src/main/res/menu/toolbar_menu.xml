<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

<!--
    <item
        android:id="@+id/buscarListas"
        android:title="Buscador"
        android:icon="@drawable/icono_busqueda"
        android:iconTint="@color/white"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="ifRoom|collapseActionView"
        >
    </item>

    <item
        android:id="@+id/filtrarListasCargue"
        android:title="Filtro cargue"
        android:icon="@drawable/baseline_filter_alt_24"
        android:iconTint="@color/white"
        app:actionViewClass="androidx.constraintlayout.utils.widget.ImageFilterButton"
        app:showAsAction="ifRoom|collapseActionView"
        >
    </item>
    -->
</menu>