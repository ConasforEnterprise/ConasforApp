<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="2dp"
        android:color="@color/verde_oscuro"/> <!-- Aquí defines el color y el ancho del borde -->
    <corners android:radius="4dp"/> <!-- Aquí defines el radio de las esquinas -->
</shape>